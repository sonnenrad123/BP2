<UserControl x:Class="ClientUI.Views.CompetitionsTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"></RowDefinition>
            <RowDefinition Height="900*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Background="Black" Opacity="0.8" Grid.Row="0">
            <Label Content="Competitions"  VerticalAlignment="Top" HorizontalAlignment="Center"  Foreground="White" FontStyle="Italic" FontFamily="Aharoni" FontSize="22"/>
        </Grid>

        <Grid Grid.Row="1" >

            <DataGrid  x:Name="tabela" AutoGenerateColumns="False" IsReadOnly="True" RowBackground="AliceBlue" ItemsSource="{Binding Path=Competitions, UpdateSourceTrigger=PropertyChanged}"  SelectedItem="{Binding SelectedCompetition}" CanUserAddRows="False"  Background="#87CEFA" Opacity="0.8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID_COMP}" Width="*" />
                    <DataGridTextColumn Header="Name" Binding="{Binding NAME_COMP}" Width="*" />
                    <DataGridTextColumn Header="Start date" Binding="{Binding DATE_START}" Width="*" />
                    <DataGridTextColumn Header="End date" Binding="{Binding DATE_END}" Width="*"/>
                    <DataGridTextColumn Header="Max competitors" Binding="{Binding MAX_COMPETITORS}" Width="*"/>
                    


                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</UserControl>
